server:
    pidfile: /tmp/nsd.pid

remote-control:
  control-enable: no

zone:
  name: {{ fqdn }}
  zonefile: /etc/zones/main.zone

{% for zone in additional_zones -%}
zone:
  name: {{ zone }}
  zonefile: /etc/zones/{{ zone }}zone
{% endfor -%}
