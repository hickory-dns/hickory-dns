; replace the trust-dns.org with your own name
$TTL 3D
@               IN      SOA     trust-dns.org. root.trust-dns.org. (
                                199609203       ; Serial
                                28800   ; Refresh
                                7200    ; Retry
                                604800  ; Expire
                                86400)  ; Minimum TTL
                        NS      trust-dns.org.

1                       PTR     localhost.
