(function() {var implementors = {};
implementors['trust_dns'] = ["impl Handler for <a class='struct' href='trust_dns/server/struct.Server.html' title='trust_dns::server::Server'>Server</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
